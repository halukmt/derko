; Route all PHP mail() calls through msmtp, which relays to Mailpit
sendmail_path = /usr/bin/msmtp -t --read-envelope-from

; Dev-friendly error settings (never use in production)
display_errors  = On
error_reporting = E_ALL
log_errors      = On
